version: "3"

services:
  db:
    image: postgres
    ports:
      - "5432:5432"
    volumes:
      - DB:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=123PoStGrEs!#$%^&*dupa!

volumes:
  DB:
